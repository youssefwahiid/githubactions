name: API Trigger

on:
  repository_dispatch:
    types: [incident_report]

jobs:
  handle-incident:
    runs-on: ubunut-latest
    steps:
      - name: print incident details
        run: echo "incident reported"
        # there is no incident only for test


    #       # curl -X POST \
    # -H "Accept: application/vnd.github+json" \
    # -H "Authorization: Bearer ---->>> put your PAT here <<<-----" \
    # -H "X-GitHub-Api-Version: 2022-11-28" \
    # https://api.github.com/repos/youssefwahiid/githubactions/dispatches \
    # -d '{"event_type":"incident_report"}'
